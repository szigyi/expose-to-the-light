package hu.szigyi.ettl.service

import org.scalatest.funsuite.AnyFunSuite

class TimelapseServiceSpec extends AnyFunSuite {

  test("when settings and capture data is in DB within range then changes camera settings and capture an image") {
//    val service = new TimelapseService()
  }
}
